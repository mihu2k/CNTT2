const routes = {
  default: '/',
  home: '/home',
  profile: '/@:nickname',
  search: '/search',
  login: '/login',
  product: '/product',
  productDetail: '/product/:slug',
};

export default routes;
